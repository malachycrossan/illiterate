#include <iostream>
using namespace std;

int main() {
    double a, b, c, d, e, f, g, h;
    cin >> a >> b >> c >> d >> e >> f;

    while(a != b != c != d != e != f != 0) {
        
    }

    return 0;
}