#include <iostream>
using namespace std;

int len, mx;

int countLists(int depth) {
    if(depth == len) {
        
    }
}

int main() {
    cin >> len >> mx;
    
    while(len != 0 && mx != 0) {
        
    }

    return 0;
}